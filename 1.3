package work1;
//Реализуйте метод selectColor() в теле которого задайте int переменную
// data с любым начальным значением. Если data меньше 10 включительно,
// то в консоль должно быть выведено сообщение “Красный”,
// если от 10 до 20 включительно, то “Желтый”, если больше 20 - “Зеленый”;
import java.util.Scanner;

public class work3 {
    public static void main(String[] args) {
        selectColor();
    }

    public static void selectColor() {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Введите значение переменной data: ");
        int data = scanner.nextInt();

        if (data <= 10) {
            System.out.println("Красный");
        } else if (data <= 20) {
            System.out.println("Желтый");
        } else {
            System.out.println("Зеленый");
        }

        scanner.close();
    }
}
